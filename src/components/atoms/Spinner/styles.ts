/* eslint-disable import/prefer-default-export */
import styled from 'styled-components/native';

import type { TSpinnerProps } from './types';

export const BaseActivityIndicator = styled.ActivityIndicator<TSpinnerProps>``;
